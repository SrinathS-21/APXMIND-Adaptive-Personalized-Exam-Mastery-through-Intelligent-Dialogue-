# ===========================================================================
#  APXMIND Dependencies
#  Migrated: Flask → FastAPI, Ollama → llama-cpp-python
# ===========================================================================

# Core
pandas
numpy==1.26.4
python-dotenv
pydantic>=2.0.0
pydantic-settings>=2.0.0

# LangChain & AI
langchain>=0.2.0
langchain_community>=0.2.0
langchain-ollama                    # Kept as fallback until GGUF models ready
langgraph>=0.2.0

# LLM Inference (NEW — replaces Ollama as primary)
llama-cpp-python>=0.3.0
huggingface-hub>=0.20.0

# FastAPI (NEW — replaces Flask)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart

# Vector Store & Embeddings
chromadb>=0.5.0
sentence-transformers

# Document Processing
pypdf

# Database
SQLAlchemy>=2.0.23
aiosqlite>=0.20.0

# Auth (NEW — replaces Supabase)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Development & Testing
pytest>=7.4.3
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
httpx>=0.27.0

# ===========================================================================
#  REMOVED (legacy):
#  - streamlit
#  - Flask, Flask-CORS, Flask-SQLAlchemy
#  - waitress
#  - psycopg2-binary
#  - langchain-openai (unused)
#  - ipykernel
# ===========================================================================
